import math   # here math is a module, which has many  mathmatical function to work with numbers. 
              # If we want to do any complex mathmatical operation in our program, we need this math module

print(round(2.9))  # for rounding a number--  mane kono number er purno rup dibe
print(round(15))
print(abs(-2.9))    # returnes the absoulate value of a number


print(math.ceil(2.2))  # math eikhane ekta object hisebe kaj korbe, er modules guloke access er jonno math  er pore .use korte hobe. 